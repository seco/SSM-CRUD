<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zhang.ssm.dao.DepartmentMapper">
	<select id="getDepartmentByName" resultType="zhang.ssm.entities.Department">
		select deptid departmentName d,deptname departmentName
		from department
		where deptname=#{departmentname}
	</select>
</mapper>